{
  "name": "words",
  "private": true,
  "scripts": {
    "dev": "vite --open",
    "build": "tsc && vite build",
    "preview": "yarn build && vite preview --open"
  },
  "dependencies": {
    "words-ui": "workspace:*"
  }
}
